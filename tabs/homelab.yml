- name: Homelab
  columns:
    - size: small
      widgets:
        - $include: ../widgets/myspeed.yml
        - $include: ../widgets/bookmarks.yml
    - size: full
      widgets:
        - $include: ../widgets/monitors.yml
